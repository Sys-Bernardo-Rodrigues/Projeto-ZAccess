# URL do backend (painel envia /api e /socket.io para este endereço).
# O servidor ZAccess usa por padrão a porta 3001. Se o seu servidor estiver na porta 3000, use:
# VITE_API_TARGET=http://localhost:3000
# VITE_API_TARGET=http://localhost:3001
